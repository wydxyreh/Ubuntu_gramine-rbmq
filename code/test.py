#!/usr/bin/env python3

#import subprocess
#subprocess.call(["ping", "www.baidu.com"])


import subprocess
#completed = subprocess.run(['rabbitmq-server'])
completed = subprocess.run(['sudo','rabbitmq-server'])
#completed = subprocess.run(['python'])
#completed = subprocess.run([''])
#completed = subprocess.run(['/usr/local/rabbitmq/./rabbitmq-server', '-1'])
#completed = subprocess.run(['ls', '-1'])

#print('returncode:', completed.returncode)



